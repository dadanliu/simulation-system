# 15. 测试、CI 与质量门禁

本文记录 `7. 测试、CI 与质量门禁` 的落地方案。

目标不是追求测试数量，而是保证登录、鉴权、权限、响应封装、异常过滤、商品主链路这些核心路径不会被改坏。

## 15.1 图例

```text
[Test]          Jest / Supertest 自动化测试
[TestingModule] NestJS 测试模块
[Guard]         AuthGuard / PermissionsGuard
[Filter]        HttpExceptionFilter
[Interceptor]   SuccessResponseInterceptor
[CI]            GitHub Actions 质量门禁
```

## 15.2 测试分层

当前 BFF 测试分两类：

```text
Unit Test
  - Service 内部规则
  - 密码 hash
  - 登录风控
  - 审计日志查询
  - 文件 URL 规则

E2E Test
  - Controller + Guard + Pipe + Filter + Interceptor
  - Supertest 发起真实 HTTP 请求
  - Cookie / CSRF / traceId / 响应 envelope 断言
```

测试入口：

```text
pnpm test:bff
  -> apps/bff package test
  -> jest --config ./jest.config.js --runInBand

pnpm test:bff:e2e
  -> apps/bff package test:e2e
  -> 只运行 *.e2e-spec.ts
```

相关文件：

- `apps/bff/package.json`
- `apps/bff/jest.config.js`
- `apps/bff/src/test/app-test-utils.ts`

## 15.3 核心 e2e 覆盖

### 登录成功链路

覆盖文件：

- `apps/bff/src/auth/auth.e2e-spec.ts`

覆盖点：

```text
[Test]
  POST /api/auth/login
      |
      v
[AuthController]
  CSRF 校验
  AuthService.login
  Set-Cookie
      |
      v
[Interceptor]
  success envelope
```

断言重点：

- 返回 `201`。
- 响应体包含 `success / data / message / traceId`。
- 写入 `next_bff_session`。
- Cookie 包含 `HttpOnly / SameSite / Max-Age`。
- 生产环境下正确追加 `Secure`。

### 未登录访问商品接口

覆盖文件：

- `apps/bff/src/commodity/commodity.e2e-spec.ts`

链路：

```text
GET /api/commodity/list
  无 session
      |
      v
[AuthGuard]
  currentUser = null
      |
      v
[Filter]
  401 unified response
```

断言重点：

- 返回 `401`。
- 响应体包含 `statusCode / success=false / traceId / path`。
- 商品 service 不会被调用。

### 无权限访问

覆盖文件：

- `apps/bff/src/commodity/commodity.e2e-spec.ts`

覆盖场景：

```text
operator 创建商品但权限服务返回 false
operator 编辑商品但权限服务返回 false
operator 查看审计日志
非 admin 即使 audit:read 通过也不能查看审计日志
```

链路：

```text
[AuthGuard]
  已登录
      |
      v
[PermissionsGuard]
  hasAllPermissionsByRoleCodes = false
      |
      v
[Filter]
  403 unified response
```

断言重点：

- 返回 `403`。
- 响应 message 为 `permission denied`。
- 业务 service 不会被调用。

### 商品创建、状态变更、删除

覆盖文件：

- `apps/bff/src/commodity/commodity.e2e-spec.ts`

成功路径：

```text
POST /api/commodity/create
PATCH /api/commodity/:id/status
DELETE /api/commodity/:id
```

失败路径：

```text
400  DTO 参数非法
400  状态变更缺 reason
400  删除商品缺 reason
400  前端伪造 operator
401  未登录访问
403  无权限访问
404  商品详情不存在
```

## 15.4 为什么 e2e 可以防 Guard / Filter / Interceptor 回归

这些测试不是直接调用 controller 方法，而是用 Supertest 发起 HTTP 请求：

```text
Supertest
  -> Nest HTTP Pipeline
  -> Middleware
  -> Guard
  -> Pipe
  -> Controller
  -> Service mock
  -> Interceptor
  -> Filter
```

所以如果改坏下面任意一层，主链路测试会失败：

- `AuthGuard` 没有正确注入 `currentUser`。
- `PermissionsGuard` 没有正确读取权限元数据。
- `ValidationPipe` 没有返回 400。
- `HttpExceptionFilter` 没有统一错误结构。
- `SuccessResponseInterceptor` 没有统一成功结构。
- CSRF 中间件错误拦截写操作。

## 15.5 CI 门禁

新增 workflow：

- `.github/workflows/bff-quality-gate.yml`

触发时机：

```text
pull_request
push main
push master
```

执行步骤：

```text
[CI]
  checkout
  setup pnpm 10.23.0
  setup Node.js 22
  pnpm install --frozen-lockfile
  pnpm lint:bff
  pnpm test:bff
  pnpm build:bff
```

质量门禁含义：

| 命令             | 目的                            |
| ---------------- | ------------------------------- |
| `pnpm lint:bff`  | 静态规则兜底，避免明显代码问题  |
| `pnpm test:bff`  | 单测 + e2e，全量覆盖 BFF 主链路 |
| `pnpm build:bff` | TypeScript / Nest 构建检查      |

## 15.6 暂不加入 format:check 的原因

当前执行：

```bash
pnpm format:check
```

会因为历史文件格式不一致失败，范围超过本次 7.1 的测试/CI 主链路目标。

因此本次 CI 不加入全仓库格式检查，避免新门禁一上线就因为历史格式问题变红。

后续如果要启用格式门禁，建议单独做一次：

```text
1. 全仓库 prettier --write
2. 单独提交格式化变更
3. 再把 pnpm format:check 加入 CI
```

## 15.7 验收结果

```text
[x] pnpm test:bff 会真实执行 Jest
[x] 登录成功链路有 e2e 覆盖
[x] 未登录访问商品接口返回 401
[x] 无权限访问返回 403
[x] 商品创建成功路径覆盖
[x] 商品状态变更成功和失败路径覆盖
[x] 商品删除成功和失败路径覆盖
[x] 失败路径包含 400 / 401 / 403 / 404
[x] CI 会运行 lint / test / build
```

本次验证命令：

```bash
pnpm test:bff
pnpm test:bff:e2e
pnpm lint:bff
pnpm build:bff
pnpm exec prettier --check .github/workflows/bff-quality-gate.yml apps/bff/package.json apps/bff/src/commodity/commodity.e2e-spec.ts
```
